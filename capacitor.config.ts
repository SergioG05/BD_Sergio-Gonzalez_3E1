import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'BD_Gonzalez_3E1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
